<%= bootstrap_form_with(model: assignment) do |form| %>
  <%= form.collection_select :course_id, @courses, :id, :name, error_key: :course, include_blank: "Select a value" %>
  <%= form.text_field :teaching_assistant_id %>
  <%= form.text_field :description %>
  <%= form.submit %>
<% end %>
